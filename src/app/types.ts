export const enum PlaybackRate {
  Default = 1.0,
  Run = 2.0,
  Coin = 5.0,
  Toggle = 10.0,
}

export const enum Speed {
  Walk = 65,
  Run = 200,
}

export const enum Light {
  Off = '0',
  On = '2',
}
